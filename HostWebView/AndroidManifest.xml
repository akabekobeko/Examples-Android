<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.akabeko.hostwebview"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="me.akabeko.hostwebview.TopMenuActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="me.akabeko.hostwebview.BasicCoordinationActivity"
            android:label="@string/top_menu_basic_coordination" >
        </activity>
        <activity
            android:name="me.akabeko.hostwebview.GoogleMapsActivity"
            android:label="@string/top_menu_google_maps_api" >
        </activity>
    </application>

</manifest>
